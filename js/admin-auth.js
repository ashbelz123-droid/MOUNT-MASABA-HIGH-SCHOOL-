(function(){'use strict';const sb=window.sb||window.supabaseClient;if(!sb)return;const form=document.getElementById('loginForm'),btn=document.getElementById('loginBtn'),error=document.getElementById('loginError');form?.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';btn.disabled=true;btn.textContent='Signing in…';try{const {data,error:authError}=await sb.auth.signInWithPassword({email:document.getElementById('email').value.trim(),password:document.getElementById('password').value});if(authError)throw authError;if(!data.session)throw new Error('Login session was not created.');location.replace('./admin-dashboard.html')}catch(err){error.textContent=err.message||'Unable to sign in.';btn.disabled=false;btn.textContent='Sign in'}})})();